name: Build Gost V2 APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.24'    # 这里升级到了 1.24 以配合最新的 gomobile
        check-latest: true

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Install Gomobile
      run: |
        go install golang.org/x/mobile/cmd/gomobile@latest
        gomobile init

    - name: Build Go Library (AAR)
      run: |
        cd gostlib
        # 强制更新 go.mod 到新版本标准，防止报错
        go mod edit -go=1.24
        go mod tidy
        gomobile bind -target=android -o ../app/src/main/libs/gostlib.aar .

    - name: Create Gradle Build Files
      run: |
        echo "buildscript { repositories { google(); mavenCentral() } dependencies { classpath 'com.android.tools.build:gradle:7.4.2' } }" > build.gradle
        echo "allprojects { repositories { google(); mavenCentral() } }" >> build.gradle
        
        echo "apply plugin: 'com.android.application'" > app/build.gradle
        echo "android { namespace 'com.example.gostproxy'; compileSdk 33; defaultConfig { applicationId 'com.example.gostproxy'; minSdk 26; targetSdk 33; versionCode 1; versionName '1.0'; } }" >> app/build.gradle
        echo "dependencies { implementation files('src/main/libs/gostlib.aar') }" >> app/build.gradle

    - name: Build APK
      run: gradle assembleDebug --no-daemon

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: gost-v2-proxy.apk
        path: app/build/outputs/apk/debug/app-debug.apk
